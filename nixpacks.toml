[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm --filter web build"]

[start]
cmd = "npx serve -s apps/web/dist -l $PORT"
